Fabric script that turns your Ubuntu box into Teamcity build agent with Mono & Rake support.

Prerequistis

 - Fabric
 - sudo rights
 - ssh access

The script 

 - installs mono
 - installs build agent
 - installs ruby/rake/albacore

 - configures agent and makes it run as a daemon
