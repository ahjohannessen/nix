Fabric script that turns your Ubuntu box into a Teamcity build agent with Mono & Rake support.

Prerequistis

 - fabric ( http://docs.fabfile.org )
 - sudo rights
 - ssh access

The script 

 - installs mono
 - installs build agent
 - installs ruby/rake/albacore

 - configures agent and makes it run as a daemon
